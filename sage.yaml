name: "cloud-cover"
description: "U-Net Cloud Coverage Estimator"
version : "1.0.4"
namespace: "sage"
authors: "Seongha Park <seongha.park@anl.gov>"
license: "MIT license"
homepage: "https://github.com/waggle-sensor/plugin-cloud-cover"
keywords: "machine learning, U-Net, image segmentation, cloud coverage, ground-based images"
source:
  architectures :
    - "linux/arm64"
    - "linux/amd64"
  url: "https://github.com/nconrad/plugin-cloud-cover.git"
  branch: "main"
inputs:
  - id: "interval"
    type: "int"
    description: "Inference interval in seconds (default = 0, as soon as the plugin can get image)"
  - id: "sampling-interval"
    type: "int"
    description: "Sampling interval between inferencing (default = -1, no sampling)"
